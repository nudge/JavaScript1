'use strict';

function calculateDogAge(puppy_age) {
  const dog_age_calculator = puppy_age * 7;
  return `Your doggie is ${dog_age_calculator} years old in dog years!`;
}
calculateDogAge(2);
calculateDogAge(5);
calculateDogAge(4);
